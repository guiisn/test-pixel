import { createStyles } from '@mantine/core';

const useStyles = createStyles((theme) => ({
  group: {
    border: `2px solid ${theme.colors.violet[4]}`,
    padding: '.4825rem',
    borderRadius: '46.322px',
    gap: '.4825rem',
    width: '15.81rem',
    maxWidth: '15.81rem',
  },
}));

export default useStyles;
